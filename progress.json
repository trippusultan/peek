{
  "project": "peek",
  "tagline": "Privacy-first web analytics in one Node process",
  "status": "round 3 — critics dispatched",
  "rounds": [
    { "round": 1, "result": "LOST visuals (desktop 0-6, mobile 2-3). Technical audit: all pass, 2 notes (XFF spoof, bounce trend)." },
    { "round": 2, "result": "Chart+panel headers WON. Lost: table headers, header density, axes (month-first, sparse grid), mobile legibility. Root causes: rangeStats cumulative-window bug (-52% fake trends), Chrome --screenshot drops count column at 390px (capture artifact, not layout)." },
    { "round": 3, "result": "PENDING — table headers, live chip, day-first labels, niceStep y-axis, CSS-sized chart text, rangeStats end-bound fix, CDP capture pipeline." }
  ],
  "metrics": {
    "snippet_gzip_bytes": 353,
    "dashboard_ttfb_ms": 12,
    "plausible_ttfb_ms": 358,
    "seed_events_30d": 4925,
    "seed_uv_30d": 1813
  },
  "privacy": "cookieless; visitor = server-side daily-rotating hash of IP+UA+salt; raw IP/UA never stored",
  "fixes_landed": [
    "rangeStats + top* queries now bounded [start, end) — trends honest (±1-18%)",
    "thead with column headers on all four tables",
    "live visitor chip (green dot, 5-min window)",
    "chart text CSS-sized 12px (readable at mobile scale)",
    "niceStep y-axis with k-suffix labels, day-first '2 Aug' dates",
    "padR 28 — zero edge bleed both viewports",
    "seed: mild 1.7x spikes, growth toward today",
    "capture.js: CDP screenshots (CLI flag was dropping count column at 390px)"
  ]
}
