{
  "name": "peek",
  "version": "1.0.0",
  "private": true,
  "description": "Privacy-first web analytics for personal sites: sub-2KB cookieless snippet, daily-rotating visitor hashes, SQLite, dark dashboard.",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "seed": "node --env-file=.env seed.js",
    "test": "node --env-file=.env test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "geoip-lite": "^1.4.10"
  }
}
